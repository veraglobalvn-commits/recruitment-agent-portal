services:
  telegram-bot-api:
    image: aiogram/telegram-bot-api:latest
    container_name: telegram-bot-api
    restart: always
    # Bind cho nginx host gọi qua localhost:8081 (proxy /tg-media/)
    ports:
      - "127.0.0.1:8081:8081"
    # Tham gia shared network để n8n container gọi qua tên `telegram-bot-api`
    networks:
      - tg_net
    environment:
      TELEGRAM_API_ID: ${TELEGRAM_API_ID}
      TELEGRAM_API_HASH: ${TELEGRAM_API_HASH}
      # KHÔNG set TELEGRAM_LOCAL=1: giữ default mode (file_path relative)
      # → HTTP pattern y hệt cloud, chỉ đổi base URL trong workflow
      # Nếu sau này muốn tối ưu (truy cập file qua filesystem thay vì HTTP) → bật flag này
      # và update workflow parse file_path absolute.
      TELEGRAM_STAT: "0"
      TELEGRAM_VERBOSITY: "1"
    volumes:
      # Persistent storage cho file cache
      - tg-bot-api-data:/var/lib/telegram-bot-api
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:8081/"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  tg-bot-api-data:
    driver: local

networks:
  tg_net:
    name: tg_net
    external: true
    # Tạo network bằng: docker network create tg_net
    # Cả n8n + telegram-bot-api compose phải khai báo cùng network này.
