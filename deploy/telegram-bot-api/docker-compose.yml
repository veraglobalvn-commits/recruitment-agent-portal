services:
  telegram-bot-api:
    image: aiogram/telegram-bot-api:latest
    container_name: telegram-bot-api
    restart: always
    # Bind cho nginx host gọi qua localhost:8082 (proxy /tg-media/)
    # Port 8081 bị chiếm bởi vikunja trên VPS này → dùng 8082
    ports:
      - "127.0.0.1:8082:8081"
    # Tham gia n8n_default để n8n gọi qua service name + có internet access
    networks:
      - n8n_default
    dns:
      - 8.8.8.8
      - 8.8.4.4
    environment:
      TELEGRAM_API_ID: ${TELEGRAM_API_ID}
      TELEGRAM_API_HASH: ${TELEGRAM_API_HASH}
      # Local mode: bypass Telegram Cloud 20MB getFile limit
      # file_path từ getFile sẽ là absolute (/var/lib/telegram-bot-api/TOKEN/...)
      # workflow cần normalize về relative để tg-file-server serve đúng
      TELEGRAM_LOCAL: "1"
      TELEGRAM_STAT: "0"
      TELEGRAM_VERBOSITY: "1"
    volumes:
      # Persistent storage cho file cache
      - tg-bot-api-data:/var/lib/telegram-bot-api
    healthcheck:
      # Server trả 404 cho / nhưng vẫn đang chạy → exit code 8 (wget 4xx)
      # Chỉ fail khi không connect được (exit code 4 = network error)
      test: ["CMD-SHELL", "wget -q --spider http://127.0.0.1:8081/ 2>&1; test $? -ne 4"]
      interval: 30s
      timeout: 5s
      retries: 3

  # File server: serve cached Telegram files qua HTTP
  # n8n download passport/avatar qua: http://file-server:9000/<TOKEN>/<file_path>
  # nginx /tg-media/ proxy qua: http://127.0.0.1:9000/<TOKEN>/<file_path>
  file-server:
    image: python:3.11-alpine
    container_name: tg-file-server
    restart: always
    working_dir: /data
    command: python -m http.server 9000
    volumes:
      - tg-bot-api-data:/data:ro
    ports:
      - "127.0.0.1:9000:9000"
    networks:
      - n8n_default

volumes:
  tg-bot-api-data:
    driver: local

networks:
  n8n_default:
    external: true
    # Network được tạo tự động bởi n8n docker-compose (project name = n8n)
    # Đảm bảo n8n đã chạy trước khi start telegram-bot-api
